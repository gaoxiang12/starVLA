run_id: lewm_oft_libero_dinov3b_wmonly_ctx3_statehist3_resboost_from220k_50k
run_root_dir: playground/Checkpoints
seed: 42
wandb_entity: your_name
wandb_project: starVLA_Libero
is_debug: false
version_id: "0.21"

framework:
  name: LeWMOFT
  qwenvl:
    base_vlm: WinKawaks/vit-tiny-patch16-224
  world_model:
    base_wm: dinov3_weights/dinov3_vitb16_pretrain_lvd1689m-73cec8be.pth
    train_encoder: false
    num_views: 2
    n_future: 2
    ctx_len: 3
    world_model_only: true
    freeze_latent_stats: true
    loss_latent_weight: 1.0
    latent_cosine_weight: 0.0

    # Keep the mature deterministic predictor fixed and learn a dedicated
    # residual correction from [-8, -4, 0] visual/state history. No action is
    # passed to either branch.
    predictor_state_dim: 0
    predictor_state_history: 3
    residual_predictor_dim: 384
    residual_predictor_depth: 4
    residual_predictor_heads: 6
    residual_predictor_ffn: 1024
    residual_predictor_sigreg_weight: 0.0
    context_correction_depth: 6
    context_correction_state_dim: 8
    context_correction_dim: 384
    context_correction_heads: 6
    context_correction_ffn: 1536
    context_correction_freeze_base: true

    visual_tokens_per_view: 16
    visual_token_dim: 384
    # Optimize exactly the reported raw normalized latent MSE in this ablation.
    visual_token_diversity_weight: 0.0
    visual_token_variance_weight: 0.0
    visual_token_min_std: 0.1
    latent_stats_momentum: 0.99
    visual_diagnostics: true
    use_state_cond: false
    use_state_probe: false
    use_progress_checker: false
    transition_mode: "off"
  action_model:
    action_model_type: MLP
    action_dim: 7
    state_dim: 7
    action_horizon: 8
    action_hidden_dim: 384
    random_execution_horizons: [1, 2, 4, 8]
    random_prefix_loss_weight: 0.0
  lang_cond:
    num_buckets: 4096
    embed_dim: 384

datasets:
  vla_data:
    dataset_py: lerobot_datasets
    data_root_dir: playground/Datasets/LEROBOT_LIBERO_DATA
    data_mix: libero_all_wm_l10_augmented_ctx3_h8
    future_obs_frames: true
    include_state: true
    include_progress: false
    action_type: delta_qpos
    sequential_step_sampling: false
    per_device_batch_size: 4
    load_all_data_for_training: true
    obs_image_size: [224, 224]
    video_backend: pyav

trainer:
  pretrained_checkpoint: playground/Checkpoints/lewm_oft_libero_dinov3b_l10aug489_spatial4x4_trainenc1e6_statecond_220k/checkpoints/steps_220000_pytorch_model.pt
  is_resume: false
  max_train_steps: 50000
  num_warmup_steps: 500
  save_interval: 5000
  eval_interval: 1000000
  learning_rate:
    base: 2.0e-04
    world_model: 2.0e-04
    task_embedding: 1.0e-04
  lr_scheduler_type: cosine_with_min_lr
  scheduler_specific_kwargs:
    min_lr: 1.0e-06
  freeze_modules: ""
  loss_scale:
    vla: 1.0
    vlm: 0.0
  max_grad_norm: 1.0
  weight_decay: 0.0
  logging_frequency: 50
  gradient_clipping: 1.0
  gradient_accumulation_steps: 8
  gradient_checkpointing: false
  optimizer:
    name: AdamW
    betas: [0.9, 0.95]
    eps: 1.0e-08
    weight_decay: 1.0e-08
